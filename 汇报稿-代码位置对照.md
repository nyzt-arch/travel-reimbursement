# 汇报稿 → 代码文件位置对照表

> 用法：汇报中每提到一个技术点，立即报出对应文件路径和大致行号，让评委看到你确实理解代码。
> 
> 注意：三人全栈参与，每人都有后端和前端文件。

---

## 成员 A：后端核心业务 + 前端复杂页面

> 后端路径：`reimbursement-back/src/main/java/com/example/reimbursementback/`
> 前端路径：`reimbursement-front/src/`

---

### A-1 项目技术栈选型

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| Spring Boot 4.1.0 + Java 17 | [pom.xml](../../reimbursement-back/pom.xml) | 第 8 行 `4.1.0`，第 30 行 `java.version 17` |
| MyBatis-Plus 3.5.16 | [pom.xml](../../reimbursement-back/pom.xml) | 第 46 行 |
| MySQL 8.x 驱动 | [pom.xml](../../reimbursement-back/pom.xml) | 第 57 行 |
| Lombok | [pom.xml](../../reimbursement-back/pom.xml) | 第 62 行，打包排除在 79-82 行 |
| Spring Boot 入口类 | [ReimbursementBackApplication.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/ReimbursementBackApplication.java) | 第 6-7 行 `@SpringBootApplication` |

---

### A-2 三层架构 + DTO/VO/Entity 分离

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| Controller 层 | [controller/ReimbursementController.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/controller/ReimbursementController.java) | 第 13-59 行 |
| Service 实现层（核心） | [service/impl/FkReimMainServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimMainServiceImpl.java) | 第 34 行 `extends ServiceImpl` |
| Mapper 层 | [mapper/FkReimMainMapper.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/mapper/FkReimMainMapper.java) | 第 20 行 `extends BaseMapper<FkReimMain>` |
| DTO 请求对象 | [dto/ReimbursementSaveDTO.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/dto/ReimbursementSaveDTO.java) | 第 8-176 行 含 4 个内部类 |
| VO 详情返回 | [vo/ReimbursementDetailVO.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/vo/ReimbursementDetailVO.java) | 第 8-176 行 含 4 个内部类 |

---

### A-3 MyBatis-Plus 深度应用

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| BaseMapper 零 SQL CRUD | [mapper/FkReimMainMapper.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/mapper/FkReimMainMapper.java) | 第 20 行 |
| LambdaQueryWrapper 类型安全查询 | [service/impl/FkReimMainServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimMainServiceImpl.java) | 第 72-97 行 6 个 if 条件动态拼接 |
| `like` 模糊查询 | 同上 | 第 75 行 `wrapper.like(FkReimMain::getReimNo, ...)` |
| `eq` 精确匹配 | 同上 | 第 84 行 `wrapper.eq(FkReimMain::getReimCompanyId, ...)` |
| `orderByDesc` 排序 | 同上 | 第 97 行 |
| 分页插件配置 | [config/MyBatisPlusConfig.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/config/MyBatisPlusConfig.java) | 第 14-20 行（C 编写，A 会讲） |
| 雪花算法主键 | [entity/business/FkReimMain.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/entity/business/FkReimMain.java) | 第 19 行 `@TableId(type = IdType.ASSIGN_ID)` |

---

### A-4 数据库设计与冗余字段

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| 主表冗余 ID+No+Name | [entity/business/FkReimMain.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/entity/business/FkReimMain.java) | 第 31-53 行 |
| 保存时补全冗余字段 | [service/impl/FkReimMainServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimMainServiceImpl.java) | 第 357-388 行 查字典表补全 |

---

### A-5 RESTful API + 统一响应 + 事务

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| 6 个报销单接口 | [controller/ReimbursementController.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/controller/ReimbursementController.java) | 第 20-58 行 GET/POST/PUT/DELETE |
| synchronized 并发控制 | 同上 | 第 34、42 行 `synchronized (ReimbursementController.class)` |
| 单号生成 RCBX+日期+序号 | [service/impl/FkReimMainServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimMainServiceImpl.java) | 第 349-353 行 |
| 级联删除 | 同上 | 第 301-309 行 先删子表再删主表 |
| @Transactional 事务 | 同上 | 第 246 行 `rollbackFor = Exception.class` |

---

### A-6 前端：ReimbursementDetail.vue（详情页核心）

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| 6 区块折叠状态管理 | [src/views/ReimbursementDetail.vue](../../reimbursement-front/src/views/ReimbursementDetail.vue) | collapsedStates reactive 对象 |
| 只读模式判断 | 同上 | `route.query.mode === 'view' \|\| status === 2` |
| 补助总金额重算 | 同上 | recalculateTotals() |
| **行程→补助联动** | 同上 | **handleUpdateTrips()** 149-257 行 |
| 新增行程自动生成补助日历 | 同上 | currentSubsidies.push({...}) |
| 删除行程清理对应补助 | 同上 | `filter(s => s.tripId !== deletedTripId)` |
| 已有单据自动保存 | 同上 | 非 NEW_ 前缀自动 saveDraft |
| 提交前校验 + 展开错误区块 | 同上 | handleSubmit() |

---

### A-7 前端：CostAllocationSection.vue（费用分摊）

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| **剩余补齐算法** | [src/components/detail/CostAllocationSection.vue](../../reimbursement-front/src/components/detail/CostAllocationSection.vue) | **rebalanceAllocations()** 72-110 行 |
| 第 1 行自动补齐 | 同上 | `1.0 - row2PlusRatioSum` |
| 均摊功能 | 同上 | handleEqualSplit() 226-272 行 |
| 比例输入联动金额 | 同上 | handleRatioInput() 131-176 行 |
| 金额输入联动比例 | 同上 | handleAmountInput() 179-223 行 |
| ATM 式金额输入 | 同上 | handleInputNumberLimit() 113-128 行 |

---

### A-8 前端：DataTable.vue（数据表格）

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| **Teleport 到 body** | [src/components/DataTable.vue](../../reimbursement-front/src/components/DataTable.vue) | `<teleport to="body">` |
| **getBoundingClientRect 定位** | 同上 | `rect.bottom + scrollY` 计算菜单位置 |
| 状态徽章 | 同上 | v-if status 0/1/2 对应草稿/审批中/已作废 |

---

## 成员 B：后端基础数据 + 前端架构体系

> 后端路径：`reimbursement-back/src/main/java/com/example/reimbursementback/`
> 前端路径：`reimbursement-front/src/`

---

### B-1 后端：基础数据平台

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| BaseDataController 6 个 GET 接口 | [controller/BaseDataController.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/controller/BaseDataController.java) | 第 18-46 行 |
| **树形结构构建算法** | [service/impl/BaseDataServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/BaseDataServiceImpl.java) | **第 61-84 行** HashMap + 双循环 O(n) |
| 第 1 遍：放入 Map 做索引 | 同上 | 第 67-70 行 |
| 第 2 遍：按 superiorId 挂载子节点 | 同上 | 第 73-81 行 |
| `@TableField(exist = false)` | [entity/base/BaseBusinessType.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/entity/base/BaseBusinessType.java) | 第 30 行 children 非数据库字段 |
| 6 个 BaseEntity | entity/base/*.java | 全部 |
| 6 个 BaseMapper | mapper/Base*Mapper.java | 全部 |

---

### B-2 前端：技术栈与工程化

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| package.json 依赖 | [package.json](../../reimbursement-front/package.json) | 第 18-22 行 vue/pinia/vue-router/axios |
| devDependencies | 同上 | 第 24-42 行 TypeScript/Vite/ESLint/Prettier |
| Vite 代理配置 | [vite.config.ts](../../reimbursement-front/vite.config.ts) | 第 22-28 行 `/api` → `localhost:8080` + rewrite |
| 路径别名 @ | 同上 | 第 17 行 `'@': './src'` |
| Vue 入口文件 | [src/main.ts](../../reimbursement-front/src/main.ts) | createApp + Pinia + Router |
| HMR 防 Store 重置 | 同上 | `import.meta.hot.accept` |

---

### B-3 前端：Pinia 状态管理

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| **Promise.all 并发加载 6 个接口** | [src/stores/baseData.ts](../../reimbursement-front/src/stores/baseData.ts) | **第 87-94 行** |
| isLoaded 缓存标记 | 同上 | 第 84 行 `if (this.isLoaded) return` |
| **后端→前端字段映射** | 同上 | **第 97-140 行** `reimCompanyId` → `id` |
| **递归 Getter 树搜索** | 同上 | **第 66-80 行** `findInTree` 递归函数 |
| 6 个 Getter 封装查找 | 同上 | 第 48-80 行 |
| reimbursementStore CRUD | [src/stores/reimbursement.ts](../../reimbursement-front/src/stores/reimbursement.ts) | initNewDetail / fetchList / saveDraft / submitSheet |
| NEW_ 前缀临时 ID | 同上 | 第 41 行 `NEW_${Date.now()}` |

---

### B-4 前端：Axios 拦截器 + API 层

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| **响应拦截器** | [src/api/request.ts](../../reimbursement-front/src/api/request.ts) | **第 16-30 行** |
| 判断 code !== 200 | 同上 | 第 20 行 |
| 解包返回 data | 同上 | 第 24 行 `return res.data` |
| 超时 10 秒 | 同上 | 第 6 行 |
| 泛型 PageResult | [src/api/reimbursement.ts](../../reimbursement-front/src/api/reimbursement.ts) | 第 18-21 行 `PageResult<T>` |
| API 函数类型签名 | 同上 | 第 24-28 行 `Promise<PageResult<Reimbursement>>` |

---

### B-5 前端：TypeScript 类型体系

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| 7 个核心 interface | [src/types/index.ts](../../reimbursement-front/src/types/index.ts) | 第 1-123 行 |
| Reimbursement 聚合类型 | 同上 | 第 95-123 行 含嵌套 trips/subsidies/allocations |
| BusinessType 递归类型 | 同上 | 第 37 行 `children?: BusinessType[]` |
| 城市类型联合类型 | 同上 | 第 22 行 `cityType: '1' \| '2' \| '3'` |

---

### B-6 前端：8 个自定义组件

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| TreeSelect 递归查找节点 | [src/components/TreeSelect.vue](../../reimbursement-front/src/components/TreeSelect.vue) | findNode 函数 深度遍历 |
| 只允许选叶子节点 | 同上 | handleSelectNode `thereSubordinateNode === '1'` return |
| 三层 v-for 渲染 | 同上 | 模板中三层嵌套循环 + 缩进 |
| FlatSelect v-model + click outside | [src/components/FlatSelect.vue](../../reimbursement-front/src/components/FlatSelect.vue) | modelValue prop + emit |
| Pagination 省略号算法 | [src/components/Pagination.vue](../../reimbursement-front/src/components/Pagination.vue) | pages computed |
| ConfirmDialog 4 种类型图标 | [src/components/ConfirmDialog.vue](../../reimbursement-front/src/components/ConfirmDialog.vue) | warning/success/danger/info SVG |
| SectionHeader 折叠动画 | [src/components/common/SectionHeader.vue](../../reimbursement-front/src/components/common/SectionHeader.vue) | collapse-icon CSS transition |
| FormField 表单容器 | [src/components/common/FormField.vue](../../reimbursement-front/src/components/common/FormField.vue) | label + error 插槽 |

---

### B-7 前端：路由设计

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| createWebHistory | [src/router/index.ts](../../reimbursement-front/src/router/index.ts) | 第 6 行 |
| 详情页新建/编辑共用组件 | 同上 | `/detail/new` 和 `/detail/:id` 映射同一组件 |
| mode=view 只读参数 | [src/views/ReimbursementDetail.vue](../../reimbursement-front/src/views/ReimbursementDetail.vue) | `route.query.mode === 'view'`（A 写的页面） |

---

## 成员 C：后端公共模块 + 前端业务全链路

> 后端路径：`reimbursement-back/src/main/java/com/example/reimbursementback/`
> 前端路径：`reimbursement-front/src/`

---

### C-1 后端：统一响应 + 全局异常 + MyBatis-Plus 配置

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| Result 泛型类定义 | [common/Result.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/common/Result.java) | 第 6-32 行 |
| 泛型 `<T>` 设计 | 同上 | 第 6 行 `public class Result<T>` |
| 成功工厂方法 | 同上 | success() / success(T data) |
| 失败工厂方法 | 同上 | error(String msg) |
| @RestControllerAdvice | [common/GlobalExceptionHandler.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/common/GlobalExceptionHandler.java) | 第 11 行 |
| @ExceptionHandler(RuntimeException) | 同上 | 第 14 行 |
| 转为 Result.error 返回 | 同上 | 第 15-16 行 |
| 分页插件 Bean 注册 | [config/MyBatisPlusConfig.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/config/MyBatisPlusConfig.java) | 第 14-20 行 |
| @MapperScan | 同上 | 第 11 行 |

---

### C-2 后端：四个子表 Service 实现

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| 行程查询（按 reimId + sortOrder 排序） | [service/impl/FkReimTripServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimTripServiceImpl.java) | getTripsByReimId() |
| 补助查询 + 批量保存日历 | [service/impl/FkReimSubsidyServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimSubsidyServiceImpl.java) | saveSubsidiesAndCalendars() |
| 补助日历查询（按 subsidyId + 日期排序） | [service/impl/FkReimSubsidyCalendarServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimSubsidyCalendarServiceImpl.java) | getCalendarsBySubsidyId() |
| 费用分摊查询（按 sortOrder 排序） | [service/impl/FkReimCostAllocationServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimCostAllocationServiceImpl.java) | getAllocationsByReimId() |

---

### C-3 前端：补助计算工具

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| **城市分级补助标准** | [src/utils/subsidyCalculator.ts](../../reimbursement-front/src/utils/subsidyCalculator.ts) | **第 7-17 行** getSubsidyStandard |
| 一线：餐100/交40/通40 | 同上 | 第 10 行 |
| 二线：餐80/交40/通40 | 同上 | 第 12 行 |
| 三线：餐50/交40/通40 | 同上 | 第 15 行 |
| **日期范围逐日生成** | 同上 | **第 20-48 行** getDatesBetween |
| 中文星期 + 包含结束日期 | 同上 | 第 31 行 `['星期日',...]`，第 33 行 `current <= end` |

---

### C-4 前端：表单校验系统

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| **完整校验函数** | [src/utils/validator.ts](../../reimbursement-front/src/utils/validator.ts) | **validateReimbursement()** 第 25-140 行 |
| 6 个必填字段校验 | 同上 | 第 30-53 行 |
| 行程日期重叠检测 | 同上 | 第 71-101 行 按出行人分组 O(n²) 两两比较 |
| 日期重叠判断公式 | 同上 | areDatesOverlapping() `s1<=e2 && e1>=s2` |
| 分摊比例容差 | 同上 | 第 129 行 `Math.abs(ratioSum - 1.0) > 0.0001` |
| 分摊金额容差 | 同上 | 第 132 行 `Math.abs(amountSum - subsidyTotal) > 0.05` |

---

### C-5 前端：金额/日期格式化

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| formatAmount 千分位 | [src/utils/formatter.ts](../../reimbursement-front/src/utils/formatter.ts) | 第 3-8 行 正则 `\B(?=(\d{3})+\b)` 插逗号 |
| formatDate 截取日期 | 同上 | 第 13-16 行 `substring(0, 10)` |
| formatPercent | 同上 | 第 28-30 行 |

---

### C-6 报销单状态流转（全局串联）

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| 保存草稿 status="0" | [service/impl/FkReimMainServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimMainServiceImpl.java) | 第 248 行（A 写的代码，C 负责讲业务） |
| 提交 status="1" | 同上 | 第 297 行 |
| 作废 status="2" | 同上 | 第 316 行 |
| 状态徽章展示 | [src/components/DataTable.vue](../../reimbursement-front/src/components/DataTable.vue) | v-if status 0/1/2（A 写的组件，C 讲业务含义） |
| 只读模式判断 | [src/views/ReimbursementDetail.vue](../../reimbursement-front/src/views/ReimbursementDetail.vue) | status === 2 作废单据只读 |

---

### C-7 提交流程全链路

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| 前端校验入口 | [src/views/ReimbursementDetail.vue](../../reimbursement-front/src/views/ReimbursementDetail.vue) | handleSubmit()（A 写的页面，C 负责讲调用链） |
| 调 validator | 同上 | `validateReimbursement(model.value)` |
| Store 调 API | [src/stores/reimbursement.ts](../../reimbursement-front/src/stores/reimbursement.ts) | submitSheet()（B 写的 Store） |
| API 调 Axios | [src/api/reimbursement.ts](../../reimbursement-front/src/api/reimbursement.ts) | `request.post('/reimbursement/submit', data)`（B 写的 API） |
| 后端 Controller synchronized | [controller/ReimbursementController.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/controller/ReimbursementController.java) | 第 40-46 行（A 写的 Controller） |
| 后端 Service 校验 | [service/impl/FkReimMainServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimMainServiceImpl.java) | 第 253-298 行（A 写的校验） |
| BigDecimal 精确比较 | 同上 | 第 288/293 行 `compareTo()` |

---

### C-8 前端：补助日历弹窗

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| 全选/取消全选联动 | [src/components/detail/SubsidyCalendarDialog.vue](../../reimbursement-front/src/components/detail/SubsidyCalendarDialog.vue) | isAllChecked computed 双向绑定 |
| 列全选（餐/交/通各自） | 同上 | isMealColChecked / isTransportColChecked / isCommColChecked |
| 勾选自动填标准金额 | 同上 | `day.mealAmount = val ? day.mealStandard : 0` |
| 金额不能超标准 | 同上 | validateAmountInput() |
| 左面板时间线 | 同上 | 模板中出发/到达日期 + 行程天数蓝色条 |

---

### C-9 临时 ID 策略 + 项目难点

| 要说的内容 | 对应文件 | 关键位置 |
|-----------|---------|---------|
| NEW_ 前缀初始化 | [src/stores/reimbursement.ts](../../reimbursement-front/src/stores/reimbursement.ts) | initNewDetail() `NEW_${Date.now()}`（B 写的 Store，C 讲策略） |
| COPY_ 复制前缀 | [src/views/ReimbursementList.vue](../../reimbursement-front/src/views/ReimbursementList.vue) | handleCopy `COPY_${Date.now()}`（C 写的列表页） |
| T_NEW_ 行程前缀 | [src/components/detail/TripSection.vue](../../reimbursement-front/src/components/detail/TripSection.vue) | `T_NEW_${Date.now()}`（C 写的行程区块） |
| 后端前缀识别 | [service/impl/FkReimMainServiceImpl.java](../../reimbursement-back/src/main/java/com/example/reimbursementback/service/impl/FkReimMainServiceImpl.java) | startsWith NEW_/COPY_/R（A 写的后端） |
| 后端生成雪花 ID | 同上 | 设置 id=null，MyBatis-Plus 自动生成 |

---

## 汇总：每个成员需要记住的核心文件

### 成员 A 的核心文件

| 优先级 | 文件 | 核心技术点 |
|--------|------|-----------|
| ⭐⭐⭐ | `FkReimMainServiceImpl.java`（后端） | BaseMapper/LambdaQW/分页/事务/单号/校验/级联删除 |
| ⭐⭐⭐ | `ReimbursementDetail.vue`（前端） | 6区块折叠/行程补助联动/提交全流程 |
| ⭐⭐ | `CostAllocationSection.vue`（前端） | 剩余补齐算法/均摊/比例金额联动 |
| ⭐⭐ | `ReimbursementController.java`（后端） | RESTful API/synchronized |
| ⭐ | `DataTable.vue`（前端） | Teleport/定位计算 |

### 成员 B 的核心文件

| 优先级 | 文件 | 核心技术点 |
|--------|------|-----------|
| ⭐⭐⭐ | `stores/baseData.ts`（前端） | Promise.all/isLoaded/字段映射/递归Getter |
| ⭐⭐⭐ | `BaseDataServiceImpl.java`（后端） | HashMap+双循环 树形构建 |
| ⭐⭐ | `TreeSelect.vue`（前端） | 递归查找/三层渲染/叶子限制 |
| ⭐⭐ | `api/request.ts`（前端） | Axios 拦截器/解包 |
| ⭐ | `vite.config.ts` + `types/index.ts`（前端） | 代理配置/类型体系 |

### 成员 C 的核心文件

| 优先级 | 文件 | 核心技术点 |
|--------|------|-----------|
| ⭐⭐⭐ | `validator.ts`（前端） | 完整校验/日期重叠/分摊容差 |
| ⭐⭐⭐ | `SubsidyCalendarDialog.vue`（前端） | 日历矩阵/全选联动/金额校验 |
| ⭐⭐ | `Result.java` + `GlobalExceptionHandler.java`（后端） | 泛型响应/全局异常 |
| ⭐⭐ | `subsidyCalculator.ts`（前端） | 城市分级/日期生成 |
| ⭐⭐ | `ReimbursementList.vue`（前端） | 列表页/复制/删除/作废操作 |
| ⭐ | `MyBatisPlusConfig.java`（后端） | 分页插件/MapperScan |
